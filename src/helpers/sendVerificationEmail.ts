import { resend } from "@/lib/resend"
import Email from "../../emails/verificationEmail"
import { ApiResponse } from "@/types/ApiResponse"


export async function sendVerificationMail(email: string, username: string, verifyCode) {
    return;
}

